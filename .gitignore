# --- secrets ---
.env

# --- Python ---
__pycache__/
*.pyc
*.pyo
*.egg-info/
.pytest_cache/

# --- virtualenvs (incl. nested ones in vendor MCPs) ---
.venv/
**/.venv/
venv/

# --- agent / framework state ---
.agent_history/
.langgraph_api/
agent_state.db
agent_state.db-journal

# --- editors ---
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db

# --- model files (too big for git) — root-anchored so it doesn't match
# accidental subfolders like MCP/flights-mcp/src/flights/models/ ---
/models/

# --- Claude Code local settings ---
.claude/

# --- vendor MCP lockfiles (regenerated by `uv sync`) ---
MCP/flights-mcp/uv.lock
MCP/flixbus-mcp/uv.lock
